as always, create beads as needed for the below

# colored bounding box
in gcp edit pro, zoomed tag images view, on the larger image at right, draw the bounding
box corresponding to the currently-selected zoomed sub-image at left.  the boundary
color should match the colored box around the zoomed sub-image, eg yellow for estimated
and green for confirmed.  all other behavior should stay the same (eg a click changes the
gcp location and thus bounding box of the sub-image) - the only change is that the user
can now see the sub-image boundary drawn on the larger image (which makes it easier to find
than the little gcp marker).

# estimated/confirmed sidecar file
i have not tested this yet, so don't know if it works at all.  on the view where the
user selects the gcpeditpro.txt file, and also the image directory, there is a place
to select a sidecar file - but i don't have such a file, and don't remember it's format,
thus i haven't tested it.  confirm that the functionality has been implemented, and
and if so, suggest how the user should bootstrap the file (eg i would think that it would
be auto-created upon saving gcp_list.txt, but i don't think that it is).  if it is not 
implemented let's discuss - i am considering switching to an extra column in the txt file
to simplify things for the user (instead of having to track another file).

# gcp edit pro plugin
is there a plugin framework for gcp edit pro?  if so, evaluate the difficulty of coverting
our zoomed view to a plugin.  basically i want to finish the changes, and then submit them
to the upstream repo as a PR, and i think it will be best received as a plugin (if such
framework exists).
